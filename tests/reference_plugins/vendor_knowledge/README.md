# Vendor Knowledge reference plugins (relocated)

The installable Acme reference provider moved to:

`examples/platform_plugins/intergrax_reference_vendor_knowledge_plugin/`

Qualification tests install that package via `acme_reference_vk_plugin_installed` in `tests/conftest.py`.
