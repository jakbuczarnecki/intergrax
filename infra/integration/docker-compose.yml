version: "3.9"

services:
  redis:
    image: redis:7.2.4
    container_name: intergrax-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    restart: unless-stopped