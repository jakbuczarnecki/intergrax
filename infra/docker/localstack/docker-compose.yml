services:
  localstack:
    image: localstack/localstack:3.8
    container_name: intergrax-localstack
    ports:
      - "4566:4566"
    environment:
      SERVICES: s3,sqs,dynamodb
    restart: unless-stopped
