services:
  mysql:
    image: mysql:8.4
    container_name: intergrax-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: intergrax
      MYSQL_DATABASE: intergrax
      MYSQL_USER: intergrax
      MYSQL_PASSWORD: intergrax
    restart: unless-stopped
