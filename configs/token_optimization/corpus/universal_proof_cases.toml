schema_version = "token-optimization-proof-corpus.v1"
corpus_id = "universal-safe-synthetic-v1"
evaluation_version = "token-10g.v1"

[[cases]]
case_id = "case-short-clean-prompt"
category = "short_clean_prompt"
description = "Short synthetic deployment-status request with no protected values."
input_case_id = "short-clean"
safe_tags = ["synthetic", "lossless"]

[cases.router]
allowed_statuses = ["no_optimization", "routed"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["clean_no_op"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = false

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-exact-duplicate-content"
category = "exact_duplicate_content"
description = "Repeated synthetic lines exercise exact deduplication only."
input_case_id = "exact-duplicates"
safe_tags = ["synthetic", "deduplication"]

[cases.router]
allowed_statuses = ["routed"]
allowed_configuration_ids = ["exact_only"]
allowed_reason_codes = ["exact_duplicates"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
required_layer_ids = ["builtin.exact_deduplication"]
allowed_layer_ids = ["builtin.exact_deduplication"]
forbidden_layer_ids = ["builtin.extractive_filtering"]
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "required"
optimized = "required"
ordering_required = true

[cases.prefix]
identity_required = true

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-noisy-tool-output"
category = "noisy_tool_output"
description = "Fifty-line synthetic tool output contains progress, warning, and error noise."
input_case_id = "noisy-tool-output"
source_type = "tool_output"
safe_tags = ["synthetic", "tool-output"]

[cases.router]
allowed_statuses = ["routed"]
allowed_configuration_ids = ["extractive_only"]
allowed_reason_codes = ["noisy_tool_output"]
review_required = false
allowed_risk = ["medium"]

[cases.pipeline]
expected_execution = "completed"
required_layer_ids = ["builtin.extractive_filtering"]
allowed_layer_ids = ["builtin.extractive_filtering"]
forbidden_layer_ids = ["builtin.exact_deduplication"]
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "required"
optimized = "required"
ordering_required = true

[cases.prefix]
identity_required = true

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-terminal-log-output"
category = "terminal_log_output"
description = "Synthetic terminal transcript has command markers, progress lines, and a typed error."
input_case_id = "terminal-log"
source_type = "terminal_output"
safe_tags = ["synthetic", "terminal"]

[cases.router]
allowed_statuses = ["routed"]
allowed_configuration_ids = ["extractive_only"]
allowed_reason_codes = ["noisy_tool_output"]
review_required = false
allowed_risk = ["medium"]

[cases.pipeline]
expected_execution = "completed"
required_layer_ids = ["builtin.extractive_filtering"]
allowed_layer_ids = ["builtin.extractive_filtering"]
forbidden_layer_ids = ["builtin.exact_deduplication"]
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "required"
optimized = "required"
ordering_required = true

[cases.prefix]
identity_required = true

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-rag-context-pack"
category = "rag_context_pack"
description = "Synthetic retrieval pack contains duplicate evidence identifiers and ranked chunks."
input_case_id = "rag-context-pack"
source_type = "rag_context_pack"
safe_tags = ["synthetic", "rag"]

[cases.router]
allowed_statuses = ["routed"]
allowed_configuration_ids = ["exact_only"]
allowed_reason_codes = ["exact_duplicates"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
required_layer_ids = ["builtin.exact_deduplication"]
allowed_layer_ids = ["builtin.exact_deduplication"]
forbidden_layer_ids = ["builtin.extractive_filtering"]
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "required"
optimized = "required"
ordering_required = true

[cases.prefix]
identity_required = true

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-protected-values"
category = "protected_url_path_hash_error"
description = "Synthetic URL, path, identifier, and error code must survive lossless deduplication."
input_case_id = "protected-values"
safe_tags = ["synthetic", "protected"]
protected_regions = [
  { kind = "url", value = "https://synthetic.invalid/proof/run" },
  { kind = "path", value = "synthetic/artifacts/fixed-run.json" },
  { kind = "identifier", value = "SYNTHETIC_RUN_001" },
  { kind = "exact_error", value = "E_SYNTHETIC_PROTECTED" },
]

[cases.router]
allowed_statuses = ["routed"]
allowed_configuration_ids = ["exact_only"]
allowed_reason_codes = ["exact_duplicates"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
required_layer_ids = ["builtin.exact_deduplication"]
allowed_layer_ids = ["builtin.exact_deduplication"]
forbidden_layer_ids = ["builtin.extractive_filtering"]
expected_fallback = false

[cases.protected]
expected_input_count = 4
expected_preserved_count = 4
expected_validation_status = "passed"
digest_equality_required = true
no_raw_protected_values = true

[cases.measurement]
baseline = "required"
optimized = "required"
ordering_required = true

[cases.prefix]
identity_required = true

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-code-heavy-content"
category = "code_heavy_content"
description = "Synthetic Python verification fragment contains path and hash markers."
input_case_id = "code-heavy"
source_type = "structured_data"
safe_tags = ["synthetic", "code"]

[cases.router]
allowed_statuses = ["no_optimization", "routed"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["clean_no_op"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = false

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-high-risk-lossy-content"
category = "high_risk_lossy_content"
description = "Synthetic security finding carries protected identifiers and requires review before lossy work."
input_case_id = "high-risk-protected"
allow_lossy = true
safe_tags = ["synthetic", "high-risk"]
protected_regions = [
  { kind = "security_warning", value = "SYNTHETIC_SECURITY_WARNING" },
  { kind = "identifier", value = "SYNTHETIC_FINDING_007" },
]

[cases.router]
allowed_statuses = ["review_required"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["protected_or_high_risk"]
review_required = true
allowed_risk = ["high"]

[cases.pipeline]
expected_execution = "not_started"
forbidden_layer_ids = ["builtin.exact_deduplication", "builtin.extractive_filtering"]
expected_fallback = false

[cases.protected]
expected_input_count = 2
expected_preserved_count = 0
expected_validation_status = "not_run"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = true

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-policy-disabled"
category = "policy_disabled"
description = "Disabled policy must block optimization and leave the pipeline untouched."
input_case_id = "policy-disabled"
policy_enabled = false
policy_profile = "off"
safe_tags = ["synthetic", "disabled"]

[cases.router]
allowed_statuses = ["blocked"]
allowed_configuration_ids = []
allowed_reason_codes = ["policy_disabled"]

[cases.pipeline]
expected_execution = "not_started"
forbidden_layer_ids = ["builtin.exact_deduplication", "builtin.extractive_filtering"]

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = false

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-measure-only"
category = "measure_only"
description = "Measure-only policy measures the selected strategy while preserving the original final content."
input_case_id = "measure-only"
policy_profile = "measure_only"
safe_tags = ["synthetic", "measure-only"]

[cases.router]
allowed_statuses = ["routed"]
allowed_configuration_ids = ["exact_only"]
allowed_reason_codes = ["exact_duplicates"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
required_layer_ids = ["builtin.exact_deduplication"]
allowed_layer_ids = ["builtin.exact_deduplication"]
forbidden_layer_ids = ["builtin.extractive_filtering"]
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "required"
optimized = "required"
ordering_required = true

[cases.prefix]
identity_required = true

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-prefix-stable-repeat"
category = "prefix_stable_repeat"
description = "Two prompt inputs retain the recorded stable prefix while their dynamic tails differ."
input_case_id = "prefix-stable-a"
safe_tags = ["synthetic", "prefix"]

[cases.router]
allowed_statuses = ["no_optimization", "routed"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["clean_no_op"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = true
same_as_case_id = "case-exact-duplicate-content"

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-prefix-changed"
category = "changed_dynamic_tail"
description = "Synthetic source/content variation changes only the dynamic tail; stable-prefix identity is evaluate-only."
input_case_id = "prefix-changed"
source_type = "system_policy"
safe_tags = ["synthetic", "prefix-control"]

[cases.router]
allowed_statuses = ["no_optimization", "routed"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["clean_no_op"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = false

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-reordered-tools"
category = "tool_payload_order"
description = "Synthetic request content lists alpha/beta labels; tool-schema identity is evaluate-only."
input_case_id = "tools-alpha-beta"
source_type = "tool_catalog"
safe_tags = ["synthetic", "tools"]

[cases.router]
allowed_statuses = ["no_optimization", "routed"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["clean_no_op"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = false

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-inner-dictionary-order"
category = "canonical_inner_payload_order"
description = "Synthetic request content varies inner keys; tool-schema canonicalization is evaluate-only."
input_case_id = "tools-inner-keys-reordered"
source_type = "tool_catalog"
safe_tags = ["synthetic", "canonicalization"]

[cases.router]
allowed_statuses = ["no_optimization", "routed"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["clean_no_op"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = false

[cases.cache]
mode = "not_applicable"

[[cases]]
case_id = "case-warm-cache"
category = "warm_cache_evidence"
description = "Warm reuse is a pass only with typed provider cache evidence."
input_case_id = "cache-control"
safe_tags = ["synthetic", "cache", "evidence"]

[cases.router]
allowed_statuses = ["no_optimization", "routed"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["clean_no_op"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "required"
optimized = "required"
ordering_required = true

[cases.prefix]
identity_required = false

[cases.cache]
mode = "warm_expected"

[[cases]]
case_id = "case-changed-prefix-cache-control"
category = "changed_prefix_negative_control"
description = "Cache negative control requires typed miss attribution; identity comparison is evaluate-only."
input_case_id = "tools-beta-alpha"
source_type = "tool_catalog"
safe_tags = ["synthetic", "cache", "control"]

[cases.router]
allowed_statuses = ["no_optimization", "routed"]
allowed_configuration_ids = ["no_optimization"]
allowed_reason_codes = ["clean_no_op"]
review_required = false
allowed_risk = ["low"]

[cases.pipeline]
expected_execution = "completed"
expected_fallback = false

[cases.protected]
expected_input_count = 0
expected_preserved_count = 0
expected_validation_status = "not_applicable"

[cases.measurement]
baseline = "not_applicable"
optimized = "not_applicable"
ordering_required = false

[cases.prefix]
identity_required = false

[cases.cache]
mode = "changed_prefix_negative_control"
same_as_case_id = "case-prefix-stable-repeat"
