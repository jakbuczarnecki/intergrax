---
description: Intergrax Harness AI — iteration workflow, session protocol (F3)
alwaysApply: false
---

# Intergrax — Iteration Instruction

Implement **Intergrax Harness AI / Agent OS**. Root `AGENTS.md` = stub (~350 tok). Full reference: `@docs/project/technical/guides/AGENT_INSTRUCTIONS.md`. Symbol lookup: `@docs/project/technical/guides/SYMBOL_INDEX.md`.

**I1/O1:** always-on `intergrax-token-budget.mdc` — input/output token budget.

## F3 — session

```text
ONE DOMAIN = ONE NEW CHAT
```

Audit / implement / closeout: one domain pair per chat. HEP steps: `@docs/project/maintainers/bootstrap/hep_step.txt`. Platform audit: `@docs/audit_results/AUDIT_PROTOCOL.md` only when operator requests audit.

## Domain read scope (when I1 allows load)

- **Read:** arch hub read-scope block + plan hub read-scope + **one** satellite (`docs/project/maintainers/plans/satellites/` or `docs/project/architecture/satellites/`).
- **Audits:** follow `docs/audit_results/AUDIT_PROTOCOL.md`; load campaign result files only when operator cites them.
- **Max:** one file >5k tok per session unless RESUME cites more.

## Never violate

```text
Tier-0 intergrax/ · Tier-1 runtime/ · Tier-2 agents/ · Tier-3 applications/
intergrax/ ↛ agents/|applications/   agents/ ↛ applications/
```

Default scope: gate maintenance `docs/project/maintainers/plans/PLATFORM_FOUNDATION.md` hub. Phase K / §6.3 only with explicit operator approval.

Repository artifacts: English. Operator chat: session language. **Do not commit** unless asked.

Verification / ADR / routing: `@docs/project/technical/guides/AGENT_INSTRUCTIONS.md` when needed.
