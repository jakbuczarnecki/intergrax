---
description: Full agent instructions — load on demand (@docs/guides/AGENT_INSTRUCTIONS.md), NOT always-on
alwaysApply: false
---

# Intergrax — AGENT_INSTRUCTIONS (on-demand reference)

**Do not assume this file is in context.** Root `AGENTS.md` is a **stub** (~350 tokens) auto-loaded by Cursor.

Read **`docs/guides/AGENT_INSTRUCTIONS.md`** only when you need:

- Full task routing table (22 domain pairs)
- Complete verification command bundle
- ADR workflow details
- Anti-patterns list

Default session rules: **`.cursor/rules/intergrax-iteration.mdc`** (~950 tokens) + stub `AGENTS.md` — sufficient for most iterations.

Invoke: `@docs/guides/AGENT_INSTRUCTIONS.md` or explicit operator request.
