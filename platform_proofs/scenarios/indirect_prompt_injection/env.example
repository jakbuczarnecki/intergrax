# Canonical real-model proof configuration (do not commit secrets)
INTERGRAX_LLM_PROVIDER=
INTERGRAX_LLM_MODEL=

# Controlled external Order Service (Docker default)
INTERGRAX_ORDER_SERVICE_URL=http://127.0.0.1:18091

# Optional proof artifact output directory
# INTERGRAX_PROOF_ARTIFACT_DIR=.tmp/proof/indirect_prompt_injection
