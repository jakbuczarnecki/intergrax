INTERGRAX BUNDLE REGISTRATION

The repository bundle has been attached to this conversation.

Before performing any analysis you must register the bundle artifacts and confirm their availability.

List all detected bundle files and classify them according to the bundle protocol.

Expected bundle artifact types:

STRUCTURE
DEP_GRAPH
CONTRACTS
ARCH_GRAPH
PATCH_ZONES
FULL_SOURCE

Only list the files.

Do not perform analysis.
Do not interpret the repository.
Do not propose implementation.

After listing the bundle artifacts stop.