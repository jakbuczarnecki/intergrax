# © Artur Czarnecki. All rights reserved.
# Tier-3 application dependency project (workspace member).
# Application source remains importable via PYTHONPATH=applications/.
# Canonical platform doc: docs/project/architecture/APPLICATION_DEPENDENCY_MODEL.md

[project]
name = "intergrax-attestation-demo"
version = "0.1.0"
description = "Attestation demo Tier-3 application"
requires-python = ">=3.12,<3.13"
dependencies = [
  "Intergrax-ai",
  "intergrax-boundary-demo-agent",
]

[tool.uv]
package = false

[tool.uv.sources]
Intergrax-ai = { workspace = true }
intergrax-boundary-demo-agent = { workspace = true }
