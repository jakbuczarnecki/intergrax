SESSION: ONE_DOMAIN_ONE_CHAT — prefer new chat per domain; never load prior domain history when switching DOMAIN.
READ_BUDGET: `@docs/project/technical/guides/audit_slices/<DOMAIN>.md`; plan hub read-scope + arch read-scope + one satellite max; grep SYS-INV IDs only; max 1 full read >5k tok/session; grep before semantic search.
OUTPUT_BUDGET: terse default (≤12 lines); full report only on operator request or milestone — AGENT_INSTRUCTIONS § Operator communication.
Harness Architecture Audit — Mode A1 (audit one domain pair, no implementation).
DOMAIN=PLATFORM_FOUNDATION
Canonical orchestrator: docs/project/maintainers/audit/ORCHESTRATOR.md
Context budget: docs/project/maintainers/audit/<DOMAIN>.md §0 (scoped reads — never load full plan files).
Session: ONE domain = ONE new chat recommended.
Skim: grep SYS-INV IDs from audit slice — do not read full SYSTEM_INVARIANTS.md
Progress: docs/audit_results/YYYY-MM-DD/progress.json (init: uv run python scripts/audit/init_architecture_audit_run.py --date YYYY-MM-DD --mode audit_only --domain <DOMAIN>).
Result: docs/audit_results/YYYY-MM-DD/<DOMAIN>.md from docs/project/maintainers/audit/TEMPLATE_DOMAIN_RESULT.md
Edit DOMAIN= line above to target basename (e.g. MEMORY, ORCHESTRATION).
Per domain: read docs/project/maintainers/audit/<DOMAIN>.md + audit slice + architecture/plan read-scope blocks; inspect code paths listed in audit prompt; run only listed gate scripts; classify P0–P4; write result file; sync P2–P4 to plan; P0/P1 rows only in plan — do not implement; update progress.json; report checkpoint.
Rules: tier boundaries AGENTS.md; safety fuse on plan/architecture/ideal conflict; no code unless operator approves doc fix; commits only on request.
Repository artifacts: English.
