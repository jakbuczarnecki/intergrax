# Audit read slices

Compact per-domain audit context + CODE_ENTRY paths. Use **instead of** loading full IDEAL + AUDIT_MAP + plan/arch.

Regenerate: `uv run python scripts/audit/generate_audit_read_slices.py`
