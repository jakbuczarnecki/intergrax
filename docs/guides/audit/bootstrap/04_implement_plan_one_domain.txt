Harness Architecture Audit — Mode B1 (implement open plan items, one domain).
DOMAIN=PLATFORM_FOUNDATION
Canonical orchestrator: docs/guides/audit/IMPLEMENT_ORCHESTRATOR.md
Skim: docs/guides/SYSTEM_INVARIANTS.md
Progress: docs/guides/audit/results/YYYY-MM-DD/progress.json (init: uv run python scripts/init_architecture_audit_run.py --date YYYY-MM-DD --mode implement_plan --domain <DOMAIN>).
Journal: docs/guides/implementation-journal/ when behavior changes.
Edit DOMAIN= line above (e.g. TOOLS, MEMORY).
Per domain: read plan + architecture for DOMAIN only; implement one coherent open P0/P1 item; verify; update plan; journal if needed; update progress.json; checkpoint.
Rules: no Phase K / §6.3; tier boundaries AGENTS.md; commits only on request.
Operator language: Polish. Repo artifacts: English.
