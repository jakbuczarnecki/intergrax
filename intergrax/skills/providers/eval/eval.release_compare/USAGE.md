# `eval.release_compare`

**Bundle:** `eval` · **Version:** 1.0.0 · **Risk:** `low`

## Purpose

Compare eval releases and export observation sets.

## How it works

eval.compare_releases + eval.summarize_release + eval.export_observations.

## How to use

eval_skill_profile(); CI eval gate hosts.

## What you get

Release-over-release regression visibility.

## Tools unlocked

| `tool_id` | Role |
|-----------|------|
| `eval.compare_releases` | Compare two releases |
| `eval.summarize_release` | Summarize release metrics |
| `eval.export_observations` | Export observation bundle |

## Related skills

- `eval.trajectory_judge`
- `ops.workflow_runner`
