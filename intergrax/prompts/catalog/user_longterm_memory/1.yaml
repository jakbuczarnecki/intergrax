id: user_longterm_memory
version: 1

locales:
  en:
    content:
      system: |
        USER LONG-TERM MEMORY

        Use the following entries as factual user memory only if they are relevant to the current question or task.
        If they are not relevant — ignore them completely and do not mention them in the answer.

        These entries represent persistent knowledge about the user, not conversation history.
      developer: null
      user_template: null

meta:
  model_family: generic
  output_schema_id: user.longterm.memory.v1
  tags:
    - memory
    - longterm
    - user
  description: Instruction shown before user long-term memory entries explaining how the model should treat them.
